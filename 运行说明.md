# 游戏运行说明

## 项目架构

本项目采用模块化设计，提供两套代码：

### 新版本（推荐 - 模块化结构）
- **优点**：代码结构清晰、易于维护和扩展
- **文件**：
  - `game_new_gui.py` - 图形界面入口
  - `game_cli.py` - 命令行入口
  - `src/` - 模块化源代码目录

### 原版本（单文件版本）
- **优点**：简单直接、单文件包含所有代码
- **文件**：
  - `game_gui.py` - 图形界面版本
  - `game.py` - 命令行版本

## 图形界面版本

### macOS 安装 Tkinter

在 macOS 上，Tkinter 可能需要通过系统 Python 或 Homebrew 安装：

**方法1：使用系统 Python（推荐）**
```bash
# 新版本
python3 game_new_gui.py

# 或原版本
python3 game_gui.py
```

**方法2：通过 Homebrew 安装 Python（推荐）**
```bash
# 安装 Homebrewnew_ Python (如果还没安装)
brew install python-tk@3.13

# 使用 Homebrew Python 运行
python3 game_gui.py
```

**方法3：使用 pyenv**
```bash
# 安装支持 Tkinter 的 Python
env PYTHON_CONFIGURE_OPTS="--with-tcltk-includes='-I/usr/local/opt/tcl-tk/include' --with-tcltk-libs='-L/usr/local/opt/tcl-tk/lib -ltcl8.6 -ltk8.6'" pyenv install 3.13.7

pyenv local 3.13.7
python game_new_gui.py
```

### 图形界面功能特点

- 🎮 **完整的 GUI 界面** - 带有文本显示区、状态面板和地图
- 🗺️ **可视化地图** - 实时显示当前位置和已访问地点
- ⚡ **快速操作按钮** - 常用指令一键执行
- 🎨 **复古终端风格** - 绿色文字黑色背景的经典终端主题
- 📊 **实时状态显示** - 角色属性一目了然

## 文字版本

如果无法运行图形界面版本，可以使用原始的文字版本：

**新版本：**
```bash
python3 game_cli.py
```

**原版本：**
```bash
python game.py
```

文字版本在任何 Python 环境中都能运行，不需要额外依赖。

## 代码结构说明

### 模块化设计优势

新版本采用了标准的 Python 项目结构：

- **src/models/** - 游戏数据模型
  - `player.py` - 玩家角色逻辑
  - `enemy.py` - 敌人逻辑  
  - `item.py` - 物品系统
  - `location.py` - 地点系统

- **src/game_engine.py** - 游戏核心引擎，负责世界初始化

- **src/gui.py** - GUI界面组件，独立于游戏逻辑

这种结构的优势：
- ✅ 代码清晰易读
- ✅ 易于维护和调试
- ✅ 便于添加新功能
- ✅ 支持单元测试
- ✅ 符合 Python 最佳实践

## 游戏特点

- 完整的 RPG 系统
- 5个可探索地点
- 5种不同的敌人
- 物品收集和使用
- 角色升级系统
- 回合制战斗

## 基本操作

- `look` - 查看当前位置
- `go 北/南/东/西` - 移动
- `status` - 查看角色状态
- `inventory` - 查看背包
- `attack` - 攻击敌人
- `take <物品>` - 拾取物品
- `use <物品>` - 使用物品
- `help` - 查看帮助
- `quit` - 退出游戏
